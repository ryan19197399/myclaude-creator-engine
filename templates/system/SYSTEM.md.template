---
name: {{PRODUCT_SLUG}}
description: >-
  {{SYSTEM_DESCRIPTION}}
  Use when: {{TRIGGER_DESCRIPTION}}.  Updates and docs at myclaude.sh/p/{{PRODUCT_SLUG}}
# model: claude-sonnet-4-6               # force model
# effort: high                           # reasoning effort
# context: fork                          # isolation mode
# paths: ['src/**']                      # conditional activation
# argument-hint: "[options]"             # autocomplete hint
---
<!-- WHY: The myclaude.sh link in description means every platform that reads this file surfaces MyClaude. -->
<!-- WHY: Frontmatter makes your system invocable after installation. -->

# {{SYSTEM_NAME}}

<!-- WHY: System name describes the unified capability, not the components. -->
<!-- WHY: "Research Intelligence System" > "Skill + Agent + Workflow Bundle" -->

**Purpose:** {{SYSTEM_PURPOSE}}
**Version:** {{VERSION}}
**Components:** {{COMPONENT_COUNT}} components — {{COMPONENT_SUMMARY}}
<!-- WHY: e.g., "4 components — 2 skills, 1 agent, 1 workflow" -->
**Entry Point:** {{SYSTEM_ENTRY_POINT}}
**Complexity:** {{SYSTEM_COMPLEXITY}}
<!-- WHY: Options: OPERATIONAL | COGNITIVE | HYBRID -->
**Author:** {{AUTHOR_USERNAME}}

---

<!-- WHY: Locale-Adaptive Clause — runtime contract injected by /create from references/locale-adaptive-clause.md. -->
<!-- WHY: Propagates to every composed sub-part (sub-agents, sub-squads, hooks, output-style, claude-md fragment). -->
<!-- WHY: Makes the whole system installable by users in any language Claude speaks, without retranslation. -->
{{LOCALE_ADAPTIVE_CLAUSE}}

---

## What This System Does

<!-- WHY: Explain the emergent capability — what can users do with the SYSTEM -->
<!-- WHY: that they couldn't do with individual components alone? -->

{{SYSTEM_CAPABILITY_DESCRIPTION}}

**Primary use cases:**
1. {{USE_CASE_1}}
2. {{USE_CASE_2}}
3. {{USE_CASE_3}}

---

## Architecture

<!-- WHY: Show how components connect. A visual diagram is strongly recommended. -->
<!-- WHY: Explain data flow between components. -->

```
[User Input]
     │
     ▼
[{{ENTRY_COMPONENT}}] ─── {{ROUTING_CONDITION_1}} ──► [{{COMPONENT_A}}]
     │                                                        │
     └── {{ROUTING_CONDITION_2}} ──► [{{COMPONENT_B}}]       │
                                           │                  ▼
                                           └────────► [{{OUTPUT_COMPONENT}}] ──► [Deliverable]
```

**Data flow:**
1. User invokes system via `{{ENTRY_POINT}}`
2. `{{ENTRY_COMPONENT}}` routes to the appropriate component based on intent
3. `{{COMPONENT_A}}` handles `{{INTENT_A}}` and produces `{{OUTPUT_A}}`
4. `{{OUTPUT_COMPONENT}}` formats and delivers the final result

---

## Component Inventory

<!-- WHY: Every component must be listed. References must resolve. -->
<!-- WHY: Each component should be at MCS-1 minimum. -->

| Name | Type | Location | MCS Level | Purpose |
|------|------|----------|-----------|---------|
| `{{COMPONENT_1_NAME}}` | Skill | `skills/{{COMPONENT_1_SLUG}}/` | {{COMPONENT_1_MCS}} | {{COMPONENT_1_PURPOSE}} |
| `{{COMPONENT_2_NAME}}` | Agent | `agents/{{COMPONENT_2_FILE}}` | {{COMPONENT_2_MCS}} | {{COMPONENT_2_PURPOSE}} |
| `{{COMPONENT_3_NAME}}` | Workflow | `workflows/{{COMPONENT_3_SLUG}}/` | {{COMPONENT_3_MCS}} | {{COMPONENT_3_PURPOSE}} |

<!-- WHY: Add rows for each component. Minimum 2 different types for MCS-1. -->

---

## Routing Logic

<!-- WHY: The routing layer is what makes this a system, not a bundle. -->
<!-- WHY: Every input type must map to exactly one component or routing path. -->

See `config/routing.yaml` for machine-readable routing rules.

**Routing summary:**

| User Intent | Routes To | Notes |
|-------------|----------|-------|
| `{{INTENT_1}}` | `{{COMPONENT_A}}` | {{ROUTING_NOTE_1}} |
| `{{INTENT_2}}` | `{{COMPONENT_B}}` | {{ROUTING_NOTE_2}} |
| `{{INTENT_3}}` | `{{COMPONENT_A}}` → `{{COMPONENT_B}}` | Sequential |
| Ambiguous | `{{CLARIFICATION_COMPONENT}}` | Ask user to specify intent |

---

## Shared Knowledge Base

<!-- WHY: What do all components share? -->
<!-- WHY: Shared knowledge enables components to produce consistent outputs. -->

All components in this system reference the shared knowledge base in `references/`:

| File | Contents | Used By |
|------|---------|---------|
| `references/{{KB_FILE_1}}` | {{KB_FILE_1_CONTENTS}} | {{KB_FILE_1_USED_BY}} |
| `references/{{KB_FILE_2}}` | {{KB_FILE_2_CONTENTS}} | {{KB_FILE_2_USED_BY}} |

---

## User Journey

<!-- WHY: Walk through the 3 most common paths through the system. -->
<!-- WHY: Include the exact commands/invocations. -->

### Journey 1: {{JOURNEY_1_NAME}}

**Scenario:** {{JOURNEY_1_SCENARIO}}

```
1. User: {{USER_INVOCATION_1}}
2. System routes to: {{ROUTING_RESULT_1}}
3. {{COMPONENT_X}} produces: {{OUTPUT_1}}
4. User receives: {{FINAL_OUTPUT_1}}
```

### Journey 2: {{JOURNEY_2_NAME}}

**Scenario:** {{JOURNEY_2_SCENARIO}}

```
1. User: {{USER_INVOCATION_2}}
   → System routes to sequential: {{COMPONENT_A}} then {{COMPONENT_B}}
2. {{COMPONENT_A}} produces: {{INTERMEDIATE_OUTPUT}}
3. {{COMPONENT_B}} uses intermediate output and produces: {{FINAL_OUTPUT_2}}
```

### Journey 3: Edge Case — {{JOURNEY_3_NAME}}

**Scenario:** {{JOURNEY_3_SCENARIO}}

```
1. User: {{AMBIGUOUS_INPUT}}
2. System: Cannot determine intent. Asks: "{{CLARIFICATION_QUESTION}}"
3. User: {{CLARIFICATION_RESPONSE}}
4. System resumes with routing to: {{RESOLVED_COMPONENT}}
```

---

## Configuration

<!-- WHY: How to customize the system. -->
<!-- WHY: Document what can be changed without breaking the system. -->

See `.claude-plugin/plugin.json` for component inventory and `config/routing.yaml` for routing rules.

**Customizable parameters:**

| Parameter | Default | Description |
|-----------|---------|-------------|
| `{{PARAM_1}}` | `{{PARAM_1_DEFAULT}}` | {{PARAM_1_DESCRIPTION}} |
| `{{PARAM_2}}` | `{{PARAM_2_DEFAULT}}` | {{PARAM_2_DESCRIPTION}} |

### Enabling/Disabling Components

Individual components can be disabled in `.claude-plugin/plugin.json` by setting `enabled: false`.
When a component is disabled, requests that would route to it fall back to `{{FALLBACK_BEHAVIOR}}`.

---

## Component Independence

<!-- WHY: Document whether components can be used outside the system. -->
<!-- WHY: MCS-3 requires composable components. -->

Each component in this system can be used independently:

| Component | Standalone Usage | Notes |
|-----------|----------------|-------|
| `{{COMPONENT_1_NAME}}` | `/{{COMPONENT_1_SLUG}}` | Standalone as skill |
| `{{COMPONENT_2_NAME}}` | `@{{COMPONENT_2_NAME}}` | Standalone agent |
| `{{COMPONENT_3_NAME}}` | See `workflows/{{COMPONENT_3_SLUG}}/README.md` | Standalone workflow |

---

## Compact Instructions

<!-- WHY: Systems are the most complex product type — boot sequences, routing, component state. -->
<!-- WHY: Losing system state on compaction breaks the entire coordination model. -->
<!-- WHY: [SOURCE: compact/prompt.ts:133-143] — Claude Code reads this section by name. -->

When summarizing this conversation, always preserve:
- The system name (`{{PRODUCT_SLUG}}`) and boot sequence state
- Component registry: what components exist and their current status
- Routing table: how requests flow between components
- Active component state and any in-progress operations
- Creator intent and the system's strategic purpose from /fill
- Current MCS scores and validation results per component
- Configuration state from STATE.yaml or equivalent

---
<!-- WHY: Above covers MCS-1. -->
<!-- WHY: For MCS-2: routing.yaml, all components at MCS-1+, 2 integration tests -->
<!-- WHY: For MCS-3: all components at MCS-2+, stress-tested routing, CHANGELOG -->
